import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.petfinderreact.myapp',
  appName: 'petfinder-react',
  webDir: 'build'
};

export default config;
